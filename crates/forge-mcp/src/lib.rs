// MCP client implementation — Phase 4
// Supports stdio and HTTP transports for connecting to MCP servers

pub mod protocol;

/// MCP client placeholder — will be implemented in Phase 4
pub struct McpClient;

impl McpClient {
    pub fn new() -> Self {
        Self
    }
}
